{
  "name": "@parallact/claude-bridge",
  "version": "3.6.1",
  "description": "OpenAI-compatible proxy for Claude, backed by a Claude CLI worker pool with MCP-based structured tool calling. Supports Claude Max OAuth tokens.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types --experimental-transform-types src/index.ts",
    "test": "node --test --experimental-strip-types"
  },
  "keywords": ["claude", "anthropic", "openai", "proxy", "bridge", "api"],
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
